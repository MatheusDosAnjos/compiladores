int if_cmd(int x) {
    if (x) {}
    if (x) {};
    if (x) x = 2;
    if (x) if (x) x=2;
    if (x+2) read x;

    if (y == 10) {
        print "Value is 10";
        y = y + 1;
    }

    if (check_value(x)) 
        print "Valid";

    if (arr[i] != 0) 
        print arr[i];
}

int if_else_cmd(int x) {
    if (x) {} else {}
    if (x) x=2; else x=3;
    if (x) x=x+2; else if(x) x=x+2; else x=3;

    if (x == 0) 
        print "Zero";
    else 
        print "Not zero";

    if (a > b) {
        print "A is greater";
    } else {
        print "B is greater or equal";
    }

    if (arr[i] > 10) 
        print "Big number";
    else 
        print "Small number";

    if (get_value() > 50) 
        print "High";
    else 
        print "Low";

    if (x > 0) {
        if (y > 0) 
            print "Both positive";
        else 
            print "x is positive but y is not";
    } else {
        print "x is not positive";
    };
}

real while_do_cmd() {
    while (x < 10) do 
        x = x + 1;

    while (x > 0) do {
        x = x - 1;
        print x;
    }

    while (i < 5) do {
        while (j < 3) do {
            j = j + 1;
        };
        i = i + 1;
    }

    while (arr[i] != 0) do 
        i = i + 1;

    while (get_value() < 100) do 
        x = x * 2;
}

byte do_while_cmd(int y) {
    do 
        x = x + 1; 
    while (x < 10);

    do {
        x = x * 2;
        print x;
    } while (x < 100);

    do {
        do {
            j = j - 1;
        } while (j > 0);
        i = i - 1;
    } while (i > 0);

    do {
        arr[i] = arr[i] + 1;
        i = i + 1;
    } while (arr[i] != 0);

    do {
        x = x + get_increment();
    } while (x < 50);
}
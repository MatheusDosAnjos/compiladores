int a = 1;
byte b = 2;
real c = 5/2;
int aa = 'a';
byte bb = 'b';

int d[2] = 3, 'c';
byte dd[2] = 4, 'd';
real ddd[2] = 5/3, 5/4;

byte func() {}
byte func1(int x, byte y, real w) {}

int main() {
    print a b c aa bb d[0] dd[0] ddd[0] func() func1(1, 'a', c);

/---
    // --- INVALID_FUNCTION_CALL_2 ---
    print func1(5 < 6);
    print func2(5/2, 5/2, 19);

    // --- INVALID_ARITHMETIC_EXPR ---
    a = a + (2 < 4);
    a = 2 - 3/5;
    a = bbb / c[1];
    bbb = bbb * a;
    bbb = bbb + ((2 <= 4) & (2 >= 4));

    // --- INVALID_RELATIONAL_EXPR ---
    a = 5/4 < 3;
    a = 5 == 3 != 6;
    a = 5 == ~(1 == 1);

    // --- INVALID_LOGICAL_EXPR ---
    a = ~5;
    a = a & 4;

    // --- INCOMPATIBLE_ASSIGN ---
    c[1] = ccc[1];
    cc[1] = ccc[1];
    ccc[1] = c[1];
    cccc[1] = 'c';
--/
}